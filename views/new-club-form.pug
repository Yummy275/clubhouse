extends layout

block content
    form(class='p-3' method='post' action='')
        div.form-group(class='mb-3')
            label.form-label(for='title') Group Name
            input#title.form-control(type='text' name='title')
        button.btn.btn-dark(type='submit' class='mt-3') Create