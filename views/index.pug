extends layout 

block content
    if user
        h1 Welcome Back To The Clubhouse
        a(href='/log-out')
    else 
        h1 Hello Stranger, Welcome To The Clubhouse
        a(href='/sign-up') Sign Up
        a(href='/log-in') Log In

